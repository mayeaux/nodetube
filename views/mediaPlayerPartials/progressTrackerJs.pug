script.


  //
  setInterval(function(){
    var mediaUniqueTag = '#{upload.uniqueTag}'

    var channelUrl = '#{upload.uploader.channelUrl}'

    var data = {
      _csrf: csrf,
      uniqueTag: mediaUniqueTag,
      channelUrl
    }

    $.ajax({
      type: 'POST',
      url: `/user/${channelUrl}/${mediaUniqueTag}/progress`,
      data,
      success: function (data) {
        console.log(data);

        $('#processing').text(`Your upload is currently ${data}% done being converted`);

      },
      error: function (err) {
        console.log(err);
      }
    });
  }, 1000 * 5);


